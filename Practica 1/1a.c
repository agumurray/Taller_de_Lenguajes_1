#include <stdio.h>
int main(){
    double pi= 3.14;
    int y= 5;
    printf("pi= %f\n", pi);
}

//si el compilador da un warning, es para avisar que hay una o varias lineas de codigo que podrian
//dar problemas en los resultados. Sin embargo, el codigo se ejecuta.

//si el compilador da un error, el programa no puede ejecutarse y no se genera el archivo .exe